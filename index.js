/**
 * @format
 */

import { AppRegistry } from 'react-native'
import App from 'src/containers/app'
import { name as appName } from 'src/app-info.json'

AppRegistry.registerComponent(appName, () => App)
